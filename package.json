{
  "name": "auth-test",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "esbuild": "^0.18.4",
    "surge": "^0.23.1"
  },
  "scripts": {
    "dev": "sirv --dev -s index.html",
    "surge": "mkdir -p out && cat index.html > out/index.html && surge ./out/ heavy-mint.surge.sh"
  },
  "devDependencies": {
    "sirv-cli": "^2.0.2"
  }
}
