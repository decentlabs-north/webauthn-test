{
  "name": "auth-test",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "esbuild": "^0.18.4",
    "surge": "^0.23.1"
  },
  "scripts": {
    "surge": "mkdir -p out && cat index.html > out/index.html && surge ./out/ heavy-mint.surge.sh"
  }

}
